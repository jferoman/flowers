<%= form_for @color do |f| %>
  <div class="form-group">
    <%= f.label :name, 'Nombre del Color' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div>
    <%= f.submit 'Editar Color' %>
  </div>
<% end %>
